package endpoint

func (e *S_Endpoint) Mount() {
	// Faz o parse da uri para trabalhar melhor
	// uri, err := url.Parse(e.URI)
	// if err != nil {
	// log.Fatalln("ERROR", "endpoint.Mount", "|", err)
	// }

	// Adiciona as queries
	// uri.Query().Add()
}
